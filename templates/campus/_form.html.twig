{{ form_start(form) }}

    <div class="uk-grid">
        <div class="uk-width-1-3"></div>
        <div class="uk-width-1-3">
        {{ form_widget(form) }}
        </div>
        <div class="uk-width-1-3"></div>
    </div>

    <div class="uk-grid">
        <div class="uk-width-1-3"></div>
        <div class="uk-width-1-3">
            <button class="uk-button uk-button-default">{{ button_label|default('Sauvegarder') }}</button>
            <a class="uk-button uk-button-default" href="{{ path('app_campus_index') }}">Retour à la liste</a>
        </div>
        <div class="uk-width-1-3"></div>
    </div>

{{ form_end(form) }}
