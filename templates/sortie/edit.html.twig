{% extends 'base.html.twig' %}

{% block title %}Edit Sortie{% endblock %}

{% block body %}

    <div class="uk-padding-large">
        <h1 class="uk-text-center">Modifier une sortie</h1>
    </div>

    {{ include('sortie/_form.html.twig', {'button_label': 'Publier la sortie'}) }}

    <div class="uk-flex uk-padding-small">
        <div class="uk-width-1-3"></div>
        <div class="uk-width-1-3">
            <a class="uk-button uk-button-default uk-width-1-1" href="{{ path('app_sortie_index') }}">Retour à la liste</a>
        </div>
        <div class="uk-width-1-3"></div>
    </div>
    <div class="uk-flex">
        <div class="uk-width-1-3"></div>
        <div class="uk-width-1-3">
            <a class="uk-button uk-button-default uk-width-1-1" href="{{ path('app_sortie_delete_page',{'id':sortie.id}) }}">Annuler cette sortie</a>
        </div>
        <div class="uk-width-1-3"></div>
    </div>



{% endblock %}
