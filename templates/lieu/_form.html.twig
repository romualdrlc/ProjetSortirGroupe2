<div class="uk-form-horizontal uk-margin-large">
    {{ form_start(form) }}

    <div class="uk-flex">
        <div class="uk-width-1-3"></div>
        <div class="uk-width-1-3">
            <div class="uk-margin">
                <label class="uk-form-label" for="form-horizontal-text">{{ field_label(form.nom) }}</label>
                <div class="uk-form-controls">
                    <input class="uk-input" id="form-horizontal-text" type="text" name="{{ field_name(form.nom) }}"
                           value="{{ field_value(form.nom) }}">
                </div>
            </div>
            <div class="uk-margin">
                <label class="uk-form-label" for="form-horizontal-text">{{ field_label(form.rue) }}</label>
                <div class="uk-form-controls">
                    <input class="uk-input" id="form-horizontal-text" type="text" name="{{ field_name(form.rue) }}"
                           value="{{ field_value(form.rue) }}">
                </div>
            </div>
            <div class="uk-margin">
                <label class="uk-form-label" for="form-horizontal-text">{{ field_label(form.latitude) }}</label>
                <div class="uk-form-controls">
                    <input class="uk-input" id="form-horizontal-text" type="text" name="{{ field_name(form.latitude) }}"
                           value="{{ field_value(form.latitude) }}">
                </div>
            </div>
            <div class="uk-margin">
                <label class="uk-form-label" for="form-horizontal-text">{{ field_label(form.longitude) }}</label>
                <div class="uk-form-controls">
                    <input class="uk-input" id="form-horizontal-text" type="text" name="{{ field_name(form.longitude) }}"
                           value="{{ field_value(form.longitude) }}">
                </div>
            </div>
            <div class="uk-margin ">
                <label class="uk-form-label" for="form-horizontal-text">{{ form_label(form.ville) }}</label>
                <div class="uk-form-controls">
                    {{ form_widget(form.ville) }}
                </div>
            </div>
        <div class="uk-width-1-3"></div>
    </div>

    </div>

<div class="uk-grid">
    <div class="uk-width-1-3"></div>
    <div class="uk-width-1-3">
        <button class="uk-button uk-button-default uk-width-1-1 uk-margin-small">{{ button_label|default('Créer') }}</button>
        <button class="uk-button uk-button-default uk-width-1-1 uk-margin-small">
            <a href="{{ path('app_sortie_new') }}">Retour à la création de sortie</a>
        </button>
    </div>
    <div class="uk-width-1-3"></div>
</div>

{{ form_end(form) }}
