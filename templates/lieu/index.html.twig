{% extends 'base.html.twig' %}

{% block title %}Lieu index{% endblock %}

{% block body %}


    <div class="uk-flex uk-padding-large">
        <div class=" uk-width-1-4"></div>
        <div class=" uk-width-2-4">
             <h1 class="uk-text-center">Lieu index</h1>
        </div>
        <div class=" uk-width-1-4"></div>
    </div>


        <div class="uk-flex">
            <div class=" uk-width-1-3"></div>
            <div class="uk-width-1-3">
                <div>
                    <div>Id</div>
                    <div>Nom</div>
                    <div>Rue</div>
                    <div>Latitude</div>
                    <div>Longitude</div>
                    <div>actions</div>
                </div>


        {% for lieu in lieus %}
            <div>
                <div>{{ lieu.id }}</div>
                <div>{{ lieu.nom }}</div>
                <div>{{ lieu.rue }}</div>
                <div>{{ lieu.latitude }}</div>
                <div>{{ lieu.longitude }}</div>
                <div>
                    <a href="{{ path('app_lieu_show', {'id': lieu.id}) }}">show</a>
                    <a href="{{ path('app_lieu_edit', {'id': lieu.id}) }}">edit</a>
                </div>
            </div>


        {% else %}
            <div>
                <div colspan="6">no records found</div>
            </div>
        {% endfor %}

            <a href="{{ path('app_lieu_new') }}">Create new</a>
            <div class=" uk-width-1-3"></div>
        </div>
{% endblock %}