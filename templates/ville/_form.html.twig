<div class="uk-form-horizontal uk-margin-large">
    {{ form_start(form) }}


    <div class="form-group">
        <input type="hidden" name="_token" id="tokenid" value="{{ csrf_token('delete') }}"/>
    </div>
    <div class="uk-margin">
        <div class="uk-form-controls">
            {{ form_row(form.nom) }}
        </div>
    </div>
    <div class="uk-margin">
        <div class="uk-form-controls">
            {{ form_row(form.codePostal) }}
        </div>
    </div>

    <div class="uk-grid">
        <div class="uk-width-1-3"></div>
        <div class="uk-width-1-3">
            <button class="uk-button uk-button-default">{{ button_label|default('Sauvegarder') }}</button>
            <a class="uk-button uk-button-default" href="{{ path('app_ville_index') }}">Retour à la liste</a>
        </div>
        <div class="uk-width-1-3"></div>
    </div>

    {{ form_end(form, {render_rest: false}) }}
</div>