{% extends 'base.html.twig' %}

{% block title %}Edit Participant{% endblock %}

{% block body %}
    <div class="uk-container">

        <h1>Mon Profil</h1>

        <div class="photo">
            <img src="{{ asset("images/profil/"~participant.photo) }}">
        </div>

        {{ include('participant/_form.html.twig', {'button_label': 'Update'}) }}

        {{ include('participant/_delete_form.html.twig', {'button_label': 'Delete'}) }}
    </div>
{% endblock %}
